# Copy this file to .env.local and fill it in. Never commit .env.local.

# Cloudinary (only the upload script uses these)
CLOUDINARY_CLOUD_NAME=
CLOUDINARY_API_KEY=
CLOUDINARY_API_SECRET=

# MongoDB Atlas (upload script AND the /api function on Vercel use these)
MONGODB_URI=
MONGODB_DB=retro_portfolio
